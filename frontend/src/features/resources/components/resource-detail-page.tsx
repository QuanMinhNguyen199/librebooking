import { ArrowLeft, ArrowRight, Building2, Check, Clock3, PackagePlus, UsersRound } from "lucide-react";
import { demoRooms } from "../mock";

export function ResourceDetailPage({ resourceId, onBack, onBook }: { resourceId: string; onBack: () => void; onBook: (resourceId: string) => void }) {
  const room = demoRooms.find(item => item.id === resourceId) ?? demoRooms[0];
  return <div><button onClick={onBack} className="mb-5 flex items-center gap-2 text-sm font-semibold text-slate-500 hover:text-[#c71927]"><ArrowLeft size={16} /> Quay lại danh mục phòng</button><div className="rounded-2xl border border-slate-200 bg-white p-6 md:p-8"><div className="flex flex-col justify-between gap-5 border-b border-slate-100 pb-6 sm:flex-row sm:items-center"><div className="flex items-start gap-4"><span className="grid h-12 w-12 place-items-center rounded-xl bg-red-50 text-[#c71927]"><Building2 size={22} /></span><div><p className="text-xs font-semibold uppercase tracking-widest text-[#c71927]">Thông tin phòng</p><h1 className="mt-1 text-2xl font-bold">{room.name}</h1><p className="mt-1 flex items-center gap-1.5 text-sm text-slate-500"><UsersRound size={15} /> {room.description}</p></div></div><button onClick={() => onBook(room.id)} className="flex items-center justify-center gap-2 rounded-xl bg-[#df1f2d] px-5 py-3 text-sm font-semibold text-white">Đặt phòng này <ArrowRight size={16} /></button></div><div className="mt-6 grid gap-5 lg:grid-cols-3"><section className="rounded-xl bg-slate-50 p-5"><h2 className="flex items-center gap-2 text-sm font-semibold"><Check size={16} className="text-emerald-600" /> Tiện nghi cố định</h2><div className="mt-4 flex flex-wrap gap-2">{room.amenities?.map(item => <span key={item} className="rounded-full border border-slate-200 bg-white px-3 py-1.5 text-xs text-slate-600">{item}</span>)}</div></section><section className="rounded-xl bg-slate-50 p-5"><h2 className="flex items-center gap-2 text-sm font-semibold"><PackagePlus size={16} className="text-amber-600" /> Thiết bị đăng ký thêm</h2><div className="mt-3 space-y-2">{room.accessories?.map(item => <div key={item.id} className="flex justify-between rounded-lg bg-white px-3 py-2 text-xs"><span>{item.name}</span><span className="text-slate-400">Còn {item.quantityAvailable}</span></div>)}</div></section><section className="rounded-xl bg-slate-50 p-5"><h2 className="flex items-center gap-2 text-sm font-semibold"><Clock3 size={16} className="text-blue-600" /> Quy tắc đặt phòng</h2><div className="mt-4 space-y-2 text-xs leading-5 text-slate-500"><p>Giờ hoạt động: 09:00–18:00</p><p>Thời lượng đề xuất: 60 phút</p><p>{room.requiresApproval ? "Reservation cần người phụ trách duyệt." : "Reservation được xác nhận ngay nếu còn trống."}</p></div></section></div></div></div>;
}
