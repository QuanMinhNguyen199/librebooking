import { Check, Clock3, X } from "lucide-react";
import type { AuthUser } from "@/features/authentication/types";

export function ApprovalsPage({ user }: { user: AuthUser }) {
  const requests = user.role === "manager" ? [["Workshop Marketing", "Phòng Saigon", "13:00–15:00"], ["Quay nội dung sản phẩm", "Thiết bị Studio", "14:00–17:00"]] : [["Họp đối tác chiến lược", "Phòng Saigon", "09:00–10:00"], ["Bảo trì máy chiếu", "Phòng Hanoi", "16:00–17:00"]];
  return <div><div className="mb-7"><p className="text-sm font-medium text-[#c71927]">Cần bạn xử lý</p><h1 className="mt-1 text-3xl font-bold uppercase tracking-wide">Yêu cầu chờ duyệt</h1><p className="mt-1 text-sm text-slate-500">Chỉ hiển thị các yêu cầu thuộc phạm vi {user.scope}.</p></div><div className="space-y-3">{requests.map(([title, resource, time]) => <article key={title} className="flex flex-col gap-4 rounded-2xl border border-slate-200 bg-white p-5 sm:flex-row sm:items-center"><span className="grid h-10 w-10 place-items-center rounded-xl bg-amber-50 text-amber-700"><Clock3 size={18} /></span><div className="min-w-0 flex-1"><p className="font-semibold">{title}</p><p className="mt-1 text-xs text-slate-400">{resource} · {time}</p></div><div className="flex gap-2"><button className="flex items-center gap-1 rounded-lg border border-slate-200 px-3 py-2 text-xs font-semibold text-slate-500"><X size={14} /> Từ chối</button><button className="flex items-center gap-1 rounded-lg bg-[#df1f2d] px-3 py-2 text-xs font-semibold text-white"><Check size={14} /> Duyệt</button></div></article>)}</div></div>;
}
